package com.microservicedesign.product.service.query;

/**
 * Query class used to fetch products from the product service.
 * This class can be extended with additional query parameters if needed.
 */
public class FindProductQuery {

    // Currently, this class is empty as it does not require any specific fields.
    // In the future, you can add fields like `productId`, `category`, or `name` for filtering purposes.

    /**
     * Default constructor for `FindProductQuery`.
     */
    public FindProductQuery() {
        // No fields to initialize at the moment.
    }
}
